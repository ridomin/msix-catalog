<Page x:Class="msix.catalog.app.Views.DeveloperAppsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:msix.catalog.app.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DeveloperAppsPage">

    <Page.DataContext>
        <viewModels:PackagesViewModel />
    </Page.DataContext>

    <Grid DataContext="{Binding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Developer Packages" FontSize="24" Margin="5" />
        <ListView ItemsSource="{Binding DeveloperPackages}" Grid.Row="1" 
            ItemTemplate="{StaticResource PackageInfoTemplate}"
            VerticalContentAlignment="Top"
            ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>

</Page>
